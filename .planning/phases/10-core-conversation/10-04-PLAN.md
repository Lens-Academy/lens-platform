---
phase: 10-core-conversation
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Human has verified the complete roleplay conversation experience works end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete roleplay conversation experience with a human tester.

Purpose: Confirm that all Phase 10 requirements are met before proceeding to Phase 11 (Assessment).
Output: Verified roleplay experience or list of issues to fix.
</objective>

<execution_context>
@/home/penguin/.claude/get-shit-done/workflows/execute-plan.md
@/home/penguin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-core-conversation/10-CONTEXT.md
@.planning/phases/10-core-conversation/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete roleplay experience</name>
  <files>None (verification only)</files>
  <action>
Present the following verification checklist to the user. This is a human-verify checkpoint -- no code changes, only testing.

**What was built (Plans 01-03):**
- Backend: SSE chat endpoint, history, complete, retry endpoints
- Frontend: RoleplaySection with briefing card, character identity, three toggles, text/voice input, completion, retry
- Integration: Module.tsx renders roleplay segments, TTS audio playback via /ws/tts

**Prerequisites:**
1. Database migration applied: `.venv/bin/alembic upgrade head`
2. Backend running: `python main.py --dev`
3. Frontend running: `cd web_frontend && npm run dev`
4. Content with a roleplay segment exists in a module (needs at least one `#### Roleplay` block with `id::`, `content::`, `ai-instructions::`, and optionally `opening-message::`)

**Test flow:**

1. **Briefing card (CONV-07):** Navigate to a module page with a roleplay segment. Verify briefing card appears above the conversation with scenario text and indigo/subtle accent styling.

2. **Opening message (CONV-08):** If the roleplay has `opening-message::`, verify the AI character sends it automatically. Character name should appear on the message (not "Tutor") with indigo accent color.

3. **Multi-turn conversation (CONV-01, CONV-02):** Type and send messages. Verify response streams with character name label. AI messages have indigo accent, student messages have gray. Send 2-3 exchanges.

4. **Toggle controls (VOICE-04, VOICE-05):** Toggle text display OFF/ON, TTS ON/OFF, input mode text/voice. Verify each toggle works independently.

5. **Push-to-talk (CONV-03):** In voice input mode, click mic to record, click again to send (immediate, no review). Test cancel (X) to discard.

6. **Persistence (CONV-04):** Refresh the page. Verify messages and toggle states persist.

7. **Completion (CONV-05, CONV-06):** Click Complete. Verify input disabled, done state shown, Try again button appears.

8. **Retry (CONV-09):** Click Try again. Verify fresh conversation starts with opening message if applicable.

Wait for user response: "approved" or description of issues.
  </action>
  <verify>User confirms "approved" or provides feedback</verify>
  <done>All Phase 10 requirements (CONV-01 through CONV-09, VOICE-04, VOICE-05) verified working by human tester</done>
</task>

</tasks>

<verification>
Human has tested all 8 test areas and confirmed they work as expected.
</verification>

<success_criteria>
All Phase 10 requirements (CONV-01 through CONV-09, VOICE-04, VOICE-05) are verified working by human tester.
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-conversation/10-04-SUMMARY.md`
</output>
